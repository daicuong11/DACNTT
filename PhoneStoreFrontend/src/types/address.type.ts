export interface AddressType {
  addressId: number
  province: string
  district: string
  ward: string
  street: string
  isDefault: boolean
}

export interface AddressFormType {
  province: string
  district: string
  ward: string
  street: string
  isDefault: boolean
}
